/*
 * These functions are the C side implementations of helper functions for 
 * ML realtime threads.
 *
 */

#include "platform.h"
#include "../include/c-common.h"

void allocate_pthread(struct GC_state *state, struct cont *cont);
