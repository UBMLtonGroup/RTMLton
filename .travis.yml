language: generic

sudo: required

matrix:
  include:
  - stage: Build RTMLton
    os:   linux
    env:  CC=gcc    MLTON_COMPILE_ARGS="-codegen c" 
    install:
      - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo apt-get -qq update;        fi
      - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo apt-get install -qq mlton; fi
    services:
      - docker
    script:
      - "docker run -i -v \"${PWD}:/RTMLton\" i386/ubuntu:xenial /bin/bash -c \"linux32 --32bit i386 /RTMLton/bin/travis-build\""
  - stage: regression-part1
    os: linux
    env: 
        -CC=gcc    MLTON_COMPILE_ARGS="-codegen c" REGRESSION="-p part-1"
        -CC=gcc    MLTON_COMPILE_ARGS="-codegen c" REGRESSION="-p part-2"
        -CC=gcc    MLTON_COMPILE_ARGS="-codegen c" REGRESSION="-p part-3"
        -CC=gcc    MLTON_COMPILE_ARGS="-codegen c" REGRESSION="-p part-4"
    install:
      - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo apt-get -qq update;        fi
      - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo apt-get install -qq mlton; fi
    services:
      - docker
    script:
      - "docker run -i -v \"${PWD}:/RTMLton\" i386/ubuntu:xenial /bin/bash -c \"linux32 --32bit i386 /RTMLton/bin/travis-regression\""
  

notifications:
  email: false
